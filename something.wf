func _update() : nil
    # gray background

    fill_screen 31, 31, 31

    # yellow rectangle. analog of "hello, world!" text

    let x:number, 16
    let y:number, 16
    let w:number, 64
    let h:number, 32
    rect x, y, w, h, 255, 255, 0
end
