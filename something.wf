func get_gray() : number
    return 31
end

# gray background
let gray:number, get_gray()
fill_screen gray, gray, gray

# yellow rectangle. analog of "hello, world!" text

let x:number, 16
let y:number, 16
let w:number, 64
let h:number, 32
rect x, y, w, h, 255, 255, 0
